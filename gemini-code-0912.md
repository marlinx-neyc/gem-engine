# GEM-V36D 數位雙生海事戰術智庫 — 全域 SSOT 代碼自我稽核與開發規範

本規範為 GEM-V36D (v36D.12.0) 精簡版數位雙生海事戰術控制台（包含 Desktop、Mobile 與 Master 端）之最高代碼標準。本版本已全面剔除「颱風專區 & 奇門預判」與「雙生大腦認知/未來三天預判」模組，專注於物理遙測、PINN 剛性防線與 Ground Truth 三方案評估。

---

## 一、 系統哲學與數據解耦原則 (Philosophy & Architecture)

1. **數據與 UI 完全解耦**：AI 運算引擎（Python/Colab/GitHub Actions）純粹作為數據生產者，輸出標準 SSOT `latest_decision.json`；前端網頁（`index.html`、`desktop.html`、`mobile.html`）純粹作為視覺渲染器，嚴禁在前端寫死業務邏輯。
2. **單一真實數據源 (SSOT Consensus)**：全站數據門檻與裁決指標，徹底以 JSON 資料為準，確保雙端顯示完全一致。

---

## 二、 全域 UI 視覺與色彩規範 (Global UI & Color Palette)

全站介面統一採用暗色科技風 (Dark Tech) 佈局，視覺元件與色彩定義如下：

* **背景主色 (`#0b1120`)**：全站頁面與外層容器底色。
* **卡片背景 (`#1e293b`) / 深色框線 (`#334155`)**：所有戰術資訊卡片之背景與邊框。
* **戰術藍 (`#38bdf8`)**：專用於區塊標題、卡片邊框高亮、AI 不確定性方差 $\sigma$ 指標與連線狀態點。
* **警戒紅 (`#ef4444`)**：專用於 VETO 觸發（未過關）、VETO 徽章、全線封島標籤與圖表警戒水平線。
* **安全綠 (`#10b981`)**：專用於 Q1 放行狀態、PASS 合格徽章與雙核精確度提升幅度 (+%)。
* **高光黃 (`#facc15`)**：**專用於戰術裁決總結結論、Ground Truth 方案 C 評估結論與關鍵極限數值**。
* **警戒橘 (`#f59e0b`)**：專用於氣旋預警標題與風速警戒水平線。
* **中性灰 (`#94a3b8` / `#334155`)**：專用於 11:00 與 16:00 垂直決策時間虛線，以及圖表網格分割線。

---

## 三、 桌面端 (`desktop.html`) 繪圖與佈局規範 (v36D.12.0)

桌面端採 32% / 68% 剛性雙欄分割佈局，大幅提升專注度：

### 1. 左側戰術控制欄 (32% 欄寬) 精簡卡片順序
1. **AI 雙核相對精確度與注意力閘卡片 (`#38bdf8` 邊框)**：包含 AI 信心指數、不確定性 $\sigma$ 方差、精確度提升幅度，以及微觀/宏觀雙色動態權重進度條。
2. **即時水文與剛性門檻比對卡片 (PINN Engine)**：包含 Hs Pier、Wind Local、UKC、FB Pier 之數值與 PASS/VETO 狀態徽章。
3. **當日船班官模燈號比對與最後判斷卡片**：官方預報與 GEM 模型預測之 07:30~15:00 時段燈號對比表格與最後裁決。

### 2. 右側動態圖表陣列 (68% 主畫廊) 繪圖規範
* **容器高度**：所有圖表容器 (`.chart-el`, `.chart-weekly-el`) 必須強制具備 `min-height: 220px`。
* **游標與互動 (Tooltip)**：所有圖表必須配置跨絲交叉準心游標 (`axisPointer: { type: 'cross' }`) 與帶有決策燈號的 Hover 資訊視窗。
* **2x2 圖表矩陣警戒線與時間線**：
  * **Hs 圖表**：紅虛線標註 `Hs 限值: 1.20m`。
  * **Wind 圖表**：橘虛線標註 `Wind 限值: 10.80m/s`。
  * **UKC 圖表**：綠虛線標註 `UKC 限值: 1.50m`。
  * **Tp 圖表**：紫虛線標註 `長浪臨界: 12.0s`。
  * **時間線**：所有 2x2 圖表必須標註 `11:00 決策點` 與 `16:00 預警點` 的中性灰 (`#94a3b8`) 垂直虛線。
* **未來七天雙軸趨勢預判圖表**：
  * 雙 Y 軸同步注入 `Hs 限值: 1.20m` (紅色虛線) 與 `Wind 限值: 10.80m/s` (橘色虛線) 警戒線與文字標籤。
  * 背景網格橫線顏色必須調降至暗灰色 (`#334155`)。
* **戰略三方案評估 (Ground Truth)**：位於右側底部，評估方案 A、B、C，方案 C 以高光黃 (`#facc15`) 高亮採納。

---

## 四、 手機端 (`mobile.html`) 輕量化卡片流規範 (v36D.12.0)

採用 100% 垂直單欄卡片流，嚴禁加載 ECharts DOM 以保證弱網極速載入：

### 100% 垂直卡片流堆疊順序 (由上至下)
1. **頂部連線狀態卡片**：包含 `● LIVE SSOT` 動態藍色呼吸燈與連線計數。
2. **Watchdog 數據過期 Banner (`#staleAlertMob`)**：遙測停滯 > 30 秒時自動彈出紅色警示。
3. **Q-Mode 戰術裁決 Banner (`#vetoBannerMob`)**：紅/綠動態底色高亮當前裁決。
4. **AI 雙核相對精確度與注意力閘卡片 (`#38bdf8` 邊框)**：AI 信心指數、$\sigma$ 方差、精確度提升幅度與進度條。
5. **即時海象數值與剛性門檻比對卡片 (PINN Engine)**：2x2 網格呈現 4 大水文指標與 PASS/VETO 徽章。
6. **當日船班官模燈號比對卡片**：官方 vs GEM 燈號對比表格。
7. **戰略三方案評估卡片 (Ground Truth)**：A、B、C 方案比較表，方案 C 專用高光黃 (`#facc15`)。
8. **裝置視角切換導覽列**：提供切換至桌面版控制台之連結。

---

## 五、 四層剛性 VETO 物理防線門檻 (SSOT VETO Matrix)

* **港池波高 ($H_{s,pier}$)**：$\le 1.20\text{ m}$ （超標即觸發 VETO 阻斷）。
* **局域陣風 ($W_{local}$)**：$\le 10.80\text{ m/s}$ （超標即觸發 VETO 阻斷）。
* **動態裕深 ($UKC$)**：$\ge 1.50\text{ m}$ （低於門檻即刻阻斷）。
* **碼頭乾舷 ($FB_{pier}$)**：$\ge 0.50\text{ m}$ （低於 0.50m 即刻阻斷）。

---

## 六、 通訊、Watchdog 與 RPC 雙向握手協定 (RPC & Fail-Safe)

1. **Watchdog 超時退避**：遙測數據停滯超過 30 秒，頂部高光 Banner 強制轉為 `🔴 數據過期 (READ_ONLY_BACKUP)`。
2. **RPC 雙向握手 (Confirm Receipt ACK)**：
   子視窗收到並同化數據後向 Master 回傳 ACK：
   ```javascript
   if (window.parent && window.parent !== window) {
       window.parent.postMessage({ type: 'confirmReceiptACK', timestamp: Date.now() }, '*');
   }